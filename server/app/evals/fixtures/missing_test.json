{
  "name": "untested validation branch",
  "title": "Reject invalid email addresses",
  "description": "Adds validation for signup.",
  "diff": "diff --git a/src/validation.py b/src/validation.py\n+++ b/src/validation.py\n@@ -1,2 +1,5 @@\n def is_valid_email(value):\n+    if \"@\" not in value:\n+        return False\n     return True\n",
  "expected_findings": [{"category": "test_coverage", "severity": "warning", "file": "src/validation.py", "line_start": 2}]
}
